CMAKE_MINIMUM_REQUIRED(VERSION 2.6 FATAL_ERROR)
CMAKE_POLICY(VERSION 2.6)
FIND_PACKAGE(Torch REQUIRED)

SET(src rawptr.c)
SET(luasrc init.lua)

ADD_TORCH_PACKAGE(rawptr "${src}" "${luasrc}" "System")
TARGET_LINK_LIBRARIES(rawptr luaT TH)
